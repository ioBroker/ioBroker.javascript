// Generated by "npm run test:blockly:update". Do not edit by hand.
// 21 block(s) from "trigger"

===== astro [Blockly.Trigger.blocks] =====
schedule({ astro: 'sunrise', shift: 0 }, async () => {
});

===== cron_builder [Blockly.Trigger.blocks] =====
'*'.toString().trim() + ' ' + '*'.toString().trim() + ' ' + '*'.toString().trim() + ' ' + '*'.toString().trim() + ' ' + '*'.toString().trim();

===== field_cron [Blockly.Trigger.blocks] =====
'* * * * *';

===== offFile [Blockly.Trigger.blocks] =====
offFile('0_userdata.0', '*');

===== on [Blockly.Trigger.blocks] =====
on({ id: 'select ID', change: 'ne' }, async (obj) => {
  let value = obj.state.val;
  let oldValue = obj.oldState.val;
});

===== on_ack_value [Blockly.Trigger.blocks] =====
if (obj.id && obj?.state && !obj.state.ack) {
  await setStateAsync(obj.id, { val: obj.state.val, ack: true });
}

===== on_ext [Blockly.Trigger.blocks] =====
on({ id: [].concat(['select ID']), change: 'ne' }, async (obj) => {
  let value = obj.state.val;
  let oldValue = obj.oldState.val;
});

===== on_ext_oid [registry] =====
(no generator - mutator or UI-only block)

===== on_ext_oid_container [registry] =====
(no generator - mutator or UI-only block)

===== on_source [Blockly.Trigger.blocks] =====
(obj.state ? obj.state.val : '');

===== onEnumMembers [Blockly.Trigger.blocks] =====
onEnumMembers('select ID', async (obj) => {
  let value = obj.state.val;
  let oldValue = obj.oldState.val;
});

===== onFile [Blockly.Trigger.blocks] =====
onFile('0_userdata.0', '*', false, async (id, fileName, size, data, mimeType) => {
});

===== onFile_data [Blockly.Trigger.blocks] =====
data;

===== onLog [Blockly.Trigger.blocks] =====
onLog('error', async (data) => {
});

===== onLog_data [Blockly.Trigger.blocks] =====
data.message;

===== onMessage [Blockly.Trigger.blocks] =====
onMessage('customMessage', async (data, callback) => {
  typeof callback === 'function' && callback({ result: true }); // default callback
});

===== onMessage_data [Blockly.Trigger.blocks] =====
data;

===== schedule [Blockly.Trigger.blocks] =====
schedule("* * * * *", async () => {
});

===== schedule_by_id [Blockly.Trigger.blocks] =====
scheduleById('select ID', async () => {
});

===== schedule_clear [Blockly.Trigger.blocks] =====
(() => { if (unnamed) { clearSchedule(unnamed); unnamed = null; }})();

===== schedule_create [Blockly.Trigger.blocks] =====
var schedule1;


schedule1 = schedule('* * * * *', async () => {
});
