// Generated by "npm run test:blockly:update". Do not edit by hand.
// 13 block(s) from "fixtures"

===== controls_if_elseif_else [fixtures/controls_if_elseif_else.xml] =====
if (1 == 2) {
  console.info('then');
} else if (true) {
  console.info('else if');
} else {
  console.error('else');
}

===== convert_date_options [fixtures/convert_date_options.xml] =====
formatDate(getDateObject('2026-08-11'), 'YYYY-MM-DD');

formatDate(getDateObject('2026-08-11'), 'WW', 'de');

((date) => { const d = new Date(Date.UTC(date.getFullYear(), date.getMonth(), date.getDate())); const dayNum = d.getUTCDay() || 7; d.setUTCDate(d.getUTCDate() + 4 - dayNum); const yearStart = new Date(Date.UTC(d.getUTCFullYear(), 0, 1)); return Math.ceil(((d.getTime() - yearStart.getTime()) / 86400000 + 1) / 7); })(getDateObject('2026-08-11'));

(() => { const v = getDateObject('2026-08-11'); return v.getHours() * 3600 + v.getMinutes() * 60 + v.getSeconds(); })();

formatTimeDiff(3600000, 'hh:mm');

JSON.stringify('x', null, 2);

===== exec_with_statement [fixtures/exec_with_statement.xml] =====
exec('ls -la', async (error, result, stderr) => {
  console.warn('done');
});
console.info('exec: ' + 'ls -la');

===== logic_multi_and_connected [fixtures/logic_multi_and_connected.xml] =====
(10 > 5) && true && (0 <= 7 && 7 < 10);

===== logic_switch_case_mutated [fixtures/logic_switch_case_mutated.xml] =====
var state;



switch (state) {
	case 'on':
  console.info('switched on');

		break;
	case 'off':
  console.info('switched off');

		break;
	default:
  console.warn('unknown');

		break;
}

===== procedures_defreturn_no_statements [fixtures/procedures_defreturn_no_statements.xml] =====
async function answer() {
  return 42;
}


await answer();

===== procedures_with_args [fixtures/procedures_with_args.xml] =====
var temperature, threshold;

async function isTooWarm(temperature, threshold) {
  return temperature > threshold;
}


if (await isTooWarm(24, 22)) {
  console.warn('too warm');
}

===== secret_login [fixtures/secret_login.xml] =====
console.info(SECRETS['My Mail Account']?.login);

===== sendto_custom_multi [fixtures/sendto_custom_multi.xml] =====
sendTo('telegram.0', 'send', {
  'host': 'localhost',
  'port': 8080,
  'payload': { "text": "hello" },
}, async (result) => {
  console.info('sent');
});
console.info('sendTo[custom] telegram.0: host: ' + 'localhost' + ', port: ' + 8080 + ', payload: ' + { "text": "hello" } + '');

===== system_delayed [fixtures/system_delayed.xml] =====
setStateDelayed('javascript.0.lamp', true, 300000, true);

getState('javascript.0.lamp', (err, state) => {
  setStateDelayed('javascript.0.lamp', state ? !state.val : true, 30000, false);
});

setStateDelayed('javascript.0.lamp', 42, true, 250, true);

createState('0_userdata.0.counter', { type: 'number', read: true, write: true }, async () => {
});

===== time_options [fixtures/time_options.xml] =====
formatDate(new Date(), 'YYYY-MM-DD hh:mm');

formatDate(new Date(), 'W', 'ru');

formatDate(new Date(), 'OO', 'de');

((date) => { const d = new Date(Date.UTC(date.getFullYear(), date.getMonth(), date.getDate())); const dayNum = d.getUTCDay() || 7; d.setUTCDate(d.getUTCDate() + 4 - dayNum); const yearStart = new Date(Date.UTC(d.getUTCFullYear(), 0, 1)); return Math.ceil(((d.getTime() - yearStart.getTime()) / 86400000 + 1) / 7); })(new Date());

(() => { const v = new Date(); return v.getHours() * 60 + v.getMinutes(); })();

/* start of week */ (() => { const d = new Date(); d.setHours(0, 0, 0, 0); return new Date(d.getFullYear(), d.getMonth(), d.getDate() - d.getDay() + (d.getDay() == 0 ? -6 : 1)).getTime(); })();

/* end of month */ (() => { const d = new Date(); d.setHours(0, 0, 0, 0); return new Date(d.getFullYear(), d.getMonth() + 1, 1).getTime() - 1; })();

===== timeouts_named [fixtures/timeouts_named.xml] =====
var coffeeTimer, pollInterval;


coffeeTimer = setTimeout(async () => {
  coffeeTimer = null;
  console.info('coffee is ready');
}, 300000);
(() => { if (coffeeTimer) { clearTimeout(coffeeTimer); coffeeTimer = null; }})();

pollInterval = setInterval(async () => {
  console.debug('poll');
}, 30000);
(() => { if (pollInterval) { clearInterval(pollInterval); pollInterval = null; }})();

===== trigger_options [fixtures/trigger_options.xml] =====
var nightly;


on({ id: [].concat(['javascript.0.first']).concat(['javascript.0.second']).concat(['javascript.0.third']), change: 'gt', ack: true }, async (obj) => {
  console.info('changed');
});

on({ id: 'javascript.0.lamp', val: false, ack: false }, async (obj) => {
  let value = obj.state.val;
  let oldValue = obj.oldState.val;
  console.warn('switched off');
});

nightly = schedule('0 3 * * *', async () => {
  console.debug('nightly job');
});
(() => { if (nightly) { clearSchedule(nightly); nightly = null; }})();

schedule({ astro: 'sunsetStart', shift: -15 }, async () => {
  console.info('dusk soon');
});

onFile('0_userdata.0', 'photo.jpg', true, async (id, fileName, size, data, mimeType) => {
});
