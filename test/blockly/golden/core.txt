// Generated by "npm run test:blockly:update". Do not edit by hand.
// 74 block(s) from "core"

===== controls_flow_statements [index.html #toolbox] =====
break;

===== controls_for [index.html #toolbox] =====
var i;


for (i = 1; i <= 10; i++) {
}

===== controls_forEach [index.html #toolbox] =====
var i;


var i_list = [];
for (var i_index in i_list) {
  i = i_list[i_index];
}

===== controls_if [index.html #toolbox] =====
if (false) {
}

===== controls_if_else [registry] =====
(no generator - mutator or UI-only block)

===== controls_if_elseif [registry] =====
(no generator - mutator or UI-only block)

===== controls_if_if [registry] =====
(no generator - mutator or UI-only block)

===== controls_ifelse [registry] =====
if (false) {
} else {
}

===== controls_repeat [registry] =====
for (var count = 0; count < 10; count++) {
}

===== controls_repeat_ext [index.html #toolbox] =====
for (var count = 0; count < 10; count++) {
}

===== controls_whileUntil [index.html #toolbox] =====
while (false) {
}

===== lists_create_empty [registry] =====
[];

===== lists_create_with [index.html #toolbox] =====
[];

===== lists_create_with_container [registry] =====
(no generator - mutator or UI-only block)

===== lists_create_with_item [registry] =====
(no generator - mutator or UI-only block)

===== lists_getIndex [index.html #toolbox] =====
var _7BlistVariable_7D;


_7BlistVariable_7D[0];

===== lists_getSublist [index.html #toolbox] =====
var _7BlistVariable_7D;


_7BlistVariable_7D.slice(0, 1);

===== lists_indexOf [index.html #toolbox] =====
var _7BlistVariable_7D;


_7BlistVariable_7D.indexOf('') + 1;

===== lists_isEmpty [index.html #toolbox] =====
![].length;

===== lists_length [index.html #toolbox] =====
[].length;

===== lists_repeat [index.html #toolbox] =====
function listsRepeat(value, n) {
  var array = [];
  for (var i = 0; i < n; i++) {
    array[i] = value;
  }
  return array;
}


listsRepeat(null, 5);

===== lists_reverse [index.html #toolbox] =====
[].slice().reverse();

===== lists_setIndex [index.html #toolbox] =====
var _7BlistVariable_7D;


_7BlistVariable_7D[0] = null;

===== lists_sort [index.html #toolbox] =====
function listsGetSortCompare(type, direction) {
  var compareFuncs = {
    'NUMERIC': function(a, b) {
        return Number(a) - Number(b); },
    'TEXT': function(a, b) {
        return String(a) > String(b) ? 1 : -1; },
    'IGNORE_CASE': function(a, b) {
        return String(a).toLowerCase() > String(b).toLowerCase() ? 1 : -1; },
  };
  var compare = compareFuncs[type];
  return function(a, b) { return compare(a, b) * direction; };
}


[].slice().sort(listsGetSortCompare("NUMERIC", 1));

===== lists_split [index.html #toolbox] =====
''.split(',');

===== logic_boolean [index.html #toolbox] =====
true;

===== logic_compare [index.html #toolbox] =====
0 == 0;

===== logic_negate [index.html #toolbox] =====
!true;

===== logic_null [index.html #toolbox] =====
null;

===== logic_operation [index.html #toolbox] =====
false && false;

===== logic_ternary [index.html #toolbox] =====
false ? null : null;

===== math_arithmetic [index.html #toolbox] =====
1 + 1;

===== math_atan2 [registry] =====
Math.atan2(0, 0) / Math.PI * 180;

===== math_change [index.html #toolbox] =====
var item;


item = (typeof item === 'number' ? item : 0) + 1;

===== math_constant [index.html #toolbox] =====
Math.PI;

===== math_constrain [index.html #toolbox] =====
Math.min(Math.max(50, 1), 100);

===== math_modulo [index.html #toolbox] =====
64 % 10;

===== math_number [index.html #toolbox] =====
0;

===== math_number_property [index.html #toolbox] =====
0 % 2 === 0;

===== math_on_list [index.html #toolbox] =====
[].reduce(function(x, y) {return x + y;}, 0);

===== math_random_float [index.html #toolbox] =====
Math.random();

===== math_random_int [index.html #toolbox] =====
function mathRandomInt(a, b) {
  if (a > b) {
    // Swap a and b to ensure a is smaller.
    var c = a;
    a = b;
    b = c;
  }
  return Math.floor(Math.random() * (b - a + 1) + a);
}


mathRandomInt(1, 100);

===== math_round [index.html #toolbox] =====
Math.round(3.1);

===== math_single [index.html #toolbox] =====
Math.sqrt(9);

===== math_trig [index.html #toolbox] =====
Math.sin(45 / 180 * Math.PI);

===== procedures_callnoreturn [registry] =====
await unnamed();

===== procedures_callreturn [registry] =====
await unnamed();

===== procedures_defnoreturn [registry] =====
async function unnamed() {
}

===== procedures_defreturn [registry] =====
async function unnamed() {
}

===== procedures_ifreturn [registry] =====
if (true) {
  return null;
}

===== procedures_mutatorarg [registry] =====
(no generator - mutator or UI-only block)

===== procedures_mutatorcontainer [registry] =====
(no generator - mutator or UI-only block)

===== text [index.html #toolbox] =====
'';

===== text_append [index.html #toolbox] =====
var item;


item += '';

===== text_changeCase [index.html #toolbox] =====
'abc'.toUpperCase();

===== text_charAt [index.html #toolbox] =====
var _7BtextVariable_7D;


_7BtextVariable_7D.charAt(0);

===== text_count [index.html #toolbox] =====
function textCount(haystack, needle) {
  if (needle.length === 0) {
    return haystack.length + 1;
  } else {
    return haystack.split(needle).length - 1;
  }
}


textCount('', '');

===== text_create_join_container [registry] =====
(no generator - mutator or UI-only block)

===== text_create_join_item [registry] =====
(no generator - mutator or UI-only block)

===== text_getSubstring [index.html #toolbox] =====
var _7BtextVariable_7D;


_7BtextVariable_7D.slice(0, 1);

===== text_indexOf [index.html #toolbox] =====
var _7BtextVariable_7D;


_7BtextVariable_7D.indexOf('abc') + 1;

===== text_isEmpty [index.html #toolbox] =====
!''.length;

===== text_join [index.html #toolbox] =====
'' + '';

===== text_length [index.html #toolbox] =====
'abc'.length;

===== text_print [registry] =====
window.alert('');

===== text_prompt [registry] =====
window.prompt('');

===== text_prompt_ext [registry] =====
window.prompt('');

===== text_replace [index.html #toolbox] =====
function textReplace(haystack, needle, replacement) {
  needle = needle.replace(/([-()\[\]{}+?*.$\^|,:#<!\\])/g, '\\$1')
                 .replace(/\x08/g, '\\x08');
  return haystack.replace(new RegExp(needle, 'g'), replacement);
}


textReplace('text', 'x', 'y');

===== text_reverse [index.html #toolbox] =====
''.split('').reverse().join('');

===== text_trim [index.html #toolbox] =====
'abc'.trim();

===== variables_get [registry] =====
var item;


item;

===== variables_get_dynamic [registry] =====
var item;


item;

===== variables_set [registry] =====
var item;


item = 0;

===== variables_set_dynamic [registry] =====
var item;


item = 0;
