# Keep LF as line endings for text files, so ESLint does not complain
* text eol=lf

# Denote all files that are truly binary and should not be modified.
*.png binary
*.jpg binary
*.ttf binary
*.zip binary